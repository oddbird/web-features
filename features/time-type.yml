name: time
description: The `time` CSS type represents a duration, expressed in seconds (`s`) or milliseconds (`ms`). The `time-percentage` CSS type represents a duration or percentage values.
spec: https://drafts.csswg.org/css-values-4/#time-value
group: css
compat_features:
  - css.types.time
  - css.types.time-percentage
