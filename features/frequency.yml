name: frequency
description: The `frequency` CSS type represents a frequency value, expressed in hertz (`Hz`), or  kilohertz (`kHz`). The `frequency-percentage` CSS type represents a frequency or percentage values.
spec: https://drafts.csswg.org/css-values-4/#frequency
group: css
compat_features:
  - css.types.frequency
  - css.types.frequency-percentage
  - css.types.frequency.hz
  - css.types.frequency.khz
